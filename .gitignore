# python bytecode
__pycache__/

# private references and scratch, never tagged
.local/

# built cox binary (make build) and release artifacts
/dist/cox
/dist/
/site/

# per-epic control dirs (event log, wake queue, sessions)
epics/*/.cox/
epics/*/.cox.closed/
epics/*/cox
/.orca/
.DS_Store
/cox/.cache/
/.codex/

# per-machine config (regenerated by cox workspace init / hooks on each machine)
/.claude/settings.json
/cox/workspace.json

# working state + company data (kept private, not part of the shareable product)
/epics/
/plans/
