# <alias> scout - as of <repo> @ <short sha>
## Architecture        -> docs/architecture/<alias>.md
## Bounded contexts    -> one bullet per context: name, actors, entry points, key dirs
## Cross-repo contracts-> exposes: ... / consumes: ... (HTTP, events, shared packages, DB schemas)
## Conventions         -> config, testing, e2e, lint, branch/deploy, taken from the repo's own docs
## Existing docs       -> what the repo already documents well (link) vs stale/missing
## Candidate decisions -> things that read like ADRs
## Self-report         -> harness, model, wall time, tokens/cost if visible, what was hard, what you could not read
